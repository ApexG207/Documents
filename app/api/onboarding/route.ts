import { env } from "cloudflare:workers";
import { NextRequest,NextResponse } from "next/server";
import { authorize,identity } from "../../lib/access";
const clean=(v:unknown,n:number)=>String(v||"").trim().slice(0,n);
export async function GET(request:NextRequest){if(!await authorize(request,"admin"))return NextResponse.json({error:"forbidden"},{status:403});const row=await env.DB.prepare("SELECT legal_name AS legalName,display_name AS displayName,timezone,contact_email AS contactEmail,onboarding_status AS onboardingStatus,terms_accepted_at AS termsAcceptedAt,privacy_accepted_at AS privacyAcceptedAt,updated_at AS updatedAt FROM academy_settings WHERE academy_id=? ORDER BY updated_at DESC LIMIT 1").bind("pilot").first();return NextResponse.json(row||{displayName:"Apex Training Center",timezone:"America/Chicago",contactEmail:identity(request),onboardingStatus:"profile"});}
export async function POST(request:NextRequest){if(!await authorize(request,"admin"))return NextResponse.json({error:"forbidden"},{status:403});const body=await request.json() as Record<string,unknown>,displayName=clean(body.displayName,100),contactEmail=clean(body.contactEmail,160),timezone=clean(body.timezone,60)||"America/Chicago";if(!displayName||!contactEmail.includes("@"))return NextResponse.json({error:"academy_profile_required"},{status:400});const now=Date.now(),id=crypto.randomUUID(),actor=identity(request)||"unknown",terms=body.acceptTerms===true?now:null,privacy=body.acceptPrivacy===true?now:null,status=terms&&privacy?"commercial-ready":"agreements";await env.DB.batch([env.DB.prepare("DELETE FROM academy_settings WHERE academy_id=?").bind("pilot"),env.DB.prepare("INSERT INTO academy_settings (id,academy_id,legal_name,display_name,timezone,contact_email,onboarding_status,terms_accepted_at,privacy_accepted_at,configured_by,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?)").bind(id,"pilot",clean(body.legalName,140),displayName,timezone,contactEmail,status,terms,privacy,actor,now,now),env.DB.prepare("INSERT INTO audit_events (id,academy_id,actor_email,action,object_type,object_id,outcome,created_at) VALUES (?,?,?,?,?,?,?,?)").bind(crypto.randomUUID(),"pilot",actor,"configure","academy_onboarding",id,status,now)]);return NextResponse.json({id,onboardingStatus:status});}
