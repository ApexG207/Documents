import { env } from "cloudflare:workers";
import { NextRequest,NextResponse } from "next/server";
import { authorize,identity } from "../../lib/access";
export async function GET(request:NextRequest){if(!await authorize(request,"admin"))return NextResponse.json({error:"forbidden"},{status:403});const rows=await env.DB.prepare("SELECT id,recipient_email AS recipientEmail,category,title,body,action_url AS actionUrl,status,scheduled_at AS scheduledAt,sent_at AS sentAt FROM notifications WHERE academy_id=? ORDER BY scheduled_at DESC LIMIT 200").bind("pilot").all();return NextResponse.json(rows.results);}
export async function POST(request:NextRequest){if(!await authorize(request,"admin"))return NextResponse.json({error:"forbidden"},{status:403});const now=Date.now(),actor=identity(request)||"unknown",monthAgo=now-30*86400000;const dormant=await env.DB.prepare("SELECT m.email,a.id AS athleteId,a.alias FROM memberships m JOIN athletes a ON a.academy_id=m.academy_id WHERE m.academy_id=? AND m.status='active' AND m.role IN ('parent','coach') AND NOT EXISTS (SELECT 1 FROM training_sessions t WHERE t.athlete_id=a.id AND t.session_date>=?) LIMIT 100").bind("pilot",monthAgo).all<{email:string;athleteId:string;alias:string}>();let queued=0;for(const row of dormant.results){const key=`training-dormant:${row.athleteId}:${new Date(now).toISOString().slice(0,7)}`;const exists=await env.DB.prepare("SELECT id FROM notifications WHERE academy_id=? AND dedupe_key=? LIMIT 1").bind("pilot",key).first();if(exists)continue;await env.DB.prepare("INSERT INTO notifications (id,academy_id,recipient_email,category,title,body,action_url,status,dedupe_key,scheduled_at,created_at) VALUES (?,?,?,?,?,?,?,?,?,?,?)").bind(crypto.randomUUID(),"pilot",row.email,"retention","Training record needs attention",`${row.alias} has no recorded training session in the last 30 days.`,"/","queued",key,now,now).run();queued++;}await env.DB.prepare("INSERT INTO product_events (id,academy_id,actor_email,event_name,object_type,object_id,properties_json,occurred_at) VALUES (?,?,?,?,?,?,?,?)").bind(crypto.randomUUID(),"pilot",actor,"retention_scan_completed","automation_run",null,JSON.stringify({queued}),now).run();return NextResponse.json({scanned:dormant.results.length,queued});}
