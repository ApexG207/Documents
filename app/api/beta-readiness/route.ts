import{env}from"cloudflare:workers";import{NextRequest,NextResponse}from"next/server";import{authorize}from"../../lib/access";export async function GET(r:NextRequest){if(!await authorize(r,"admin"))return NextResponse.json({error:"forbidden"},{status:403});const e=env as unknown as Record<string,string|undefined>,checks=[{gate:"G1",name:"Identity, guardian and access control",pass:Boolean(e.MATIQ_BOOTSTRAP_OWNER_EMAIL),evidence:e.MATIQ_BOOTSTRAP_OWNER_EMAIL?"bootstrap authority configured":"bootstrap owner not configured"},{gate:"G2",name:"Verification email operations",pass:Boolean(e.RESEND_API_KEY&&e.MATIQ_FROM_EMAIL),evidence:e.RESEND_API_KEY&&e.MATIQ_FROM_EMAIL?"provider and sender configured":"transactional email incomplete"},{gate:"G3",name:"Multi-tenant isolation",pass:e.MATIQ_TENANT_ISOLATION_V2==="enforced",evidence:e.MATIQ_TENANT_ISOLATION_V2||"legacy pilot routes remain"},{gate:"G4",name:"Revenue and legal operations",pass:Boolean(e.STRIPE_SECRET_KEY&&e.STRIPE_WEBHOOK_SECRET),evidence:e.STRIPE_SECRET_KEY&&e.STRIPE_WEBHOOK_SECRET?"billing configured":"billing credentials incomplete"},{gate:"G5",name:"Controlled beta authority",pass:e.MATIQ_BETA_APPROVED==="true",evidence:e.MATIQ_BETA_APPROVED==="true"?"beta approved":"cohort approval not recorded"}],ready=checks.every(x=>x.pass);return NextResponse.json({decision:ready?"GO":"HOLD",ready,score:Math.round(checks.filter(x=>x.pass).length/checks.length*100),checks,assessedAt:Date.now()},{headers:{"cache-control":"no-store"}})}
