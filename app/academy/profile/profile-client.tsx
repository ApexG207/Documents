"use client";
import {FormEvent,useEffect,useState} from "react";
type Row=Record<string,unknown>;
const parsed=(v:unknown,f:unknown)=>{try{return typeof v==="string"?JSON.parse(v):v||f}catch{return f}};
export default function AcademyProfileClient(){const[profile,setProfile]=useState<Row>({}),[notice,setNotice]=useState("");async function load(){const r=await fetch("/api/academies/profile"),data=await r.json().catch(()=>({}));if(r.ok)setProfile(data.profile||{});else setNotice(String(data.error||"Academy authority required."))}useEffect(()=>{void load()},[]);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const r=await fetch("/api/academies/profile",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))}),data=await r.json().catch(()=>({}));setNotice(r.ok?"Academy profile submitted for publication review.":String(data.error||"Profile could not be saved."));if(r.ok)void load()}const links=parsed(profile.socialLinks,{}) as Record<string,string>,teams=parsed(profile.teamAffiliations,[]) as string[];return <main className="academy-profile-shell"><header><div><p>ACADEMY ENTITY PROFILE</p><h1>Identity, affiliations, and media</h1><span>Maintain the information presented on the academy discovery card.</span></div><a href="/academy">Authority controls →</a></header>{notice&&<div className="academy-notice" role="status">{notice}</div>}<form onSubmit={save}><div><p>ENTITY IDENTITY</p><h2>Academy information</h2><label>Legal name<input name="legalName" defaultValue={String(profile.legal_name||"")}/></label><label>Display name<input name="displayName" required defaultValue={String(profile.display_name||"")}/></label><label>Description<textarea name="description" required defaultValue={String(profile.description||"")}/></label><label>Team affiliation(s)<textarea name="teamAffiliations" defaultValue={teams.join("\n")} placeholder="One team, association, or lineage per line"/></label></div><div><p>LOCATION</p><h2>Facility information</h2><label>Address<input name="addressLine1" required defaultValue={String(profile.address_line_1||"")}/></label><div className="network-pair"><label>City<input name="city" required defaultValue={String(profile.city||"")}/></label><label>State / Region<input name="region" required defaultValue={String(profile.region||"")}/></label></div><label>Postal code<input name="postalCode" required defaultValue={String(profile.postal_code||"")}/></label><label>Phone<input name="phone" defaultValue={String(profile.phone||"")}/></label></div><div><p>SOCIAL MEDIA</p><h2>Official links</h2><label>Website<input name="socialWebsite" type="url" defaultValue={links.website||String(profile.website||"")}/></label><label>Instagram<input name="socialInstagram" type="url" defaultValue={links.instagram||""}/></label><label>Facebook<input name="socialFacebook" type="url" defaultValue={links.facebook||""}/></label><label>TikTok<input name="socialTiktok" type="url" defaultValue={links.tiktok||""}/></label><label>YouTube<input name="socialYoutube" type="url" defaultValue={links.youtube||""}/></label></div><button>Save governed academy profile</button></form></main>}
