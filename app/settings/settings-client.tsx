"use client";
import {FormEvent,useEffect,useState} from "react";
type RequestState={status?:string;scheduledFor?:number;legalHold?:boolean};
export default function SettingsClient(){const[state,setState]=useState<RequestState>({}),[notice,setNotice]=useState("");async function load(){const r=await fetch("/api/account/deletion");if(r.ok)setState(await r.json())}useEffect(()=>{void load()},[]);async function schedule(e:FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget)),r=await fetch("/api/account/deletion",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)}),b=await r.json();setNotice(r.ok?"Deletion request recorded.":String(b.error||"Request failed."));await load()}async function cancel(){const r=await fetch("/api/account/deletion",{method:"DELETE"});setNotice(r.ok?"Deletion request cancelled.":"Unable to cancel request.");await load()}return <main className="settings-shell"><header><div><p>ACCOUNT CONTROL</p><h1>Privacy, safety and deletion</h1><span>Control your account, safety boundaries, and personal information.</span></div><a href="/">Return to matIQ →</a></header>{notice&&<div className="settings-notice" role="status">{notice}</div>}<section className="settings-grid"><article><p>SAFETY</p><h2>Community protections</h2><ul><li>Report unsafe or objectionable content from its record.</li><li>Block users to prevent future contact and discovery.</li><li>Minor-safety reports receive urgent review priority.</li><li>Guardian consent and academy verification remain independently governed.</li></ul><a href="mailto:safety@matiq.app">Contact safety support</a></article><form onSubmit={schedule}><p>DELETE ACCOUNT</p><h2>Permanently remove your account</h2><p>Deletion begins after a seven-day protection period. Records under an active legal or safeguarding hold remain restricted until the hold is released; all other personal data is deleted or irreversibly anonymized.</p>{state.status&&state.status!=="none"?<div className="deletion-state"><b>{state.status}</b>{state.scheduledFor&&<span>Scheduled: {new Date(state.scheduledFor).toLocaleDateString()}</span>}{state.legalHold&&<span>Protected hold applies</span>}</div>:<><label>Optional reason<textarea name="reason" maxLength={500}/></label><label className="confirm">Type DELETE to confirm<input name="confirmation" required pattern="DELETE" autoComplete="off"/></label><button className="danger">Request permanent deletion</button></>}{state.status==="scheduled"&&<button type="button" onClick={cancel}>Cancel deletion request</button>}</form></section></main>}
